# Listen on all interfaces so other devices on your LAN can connect
listener 1883 0.0.0.0

# Start simple (no auth). Add auth/TLS later.
allow_anonymous true

persistence true
persistence_location /mosquitto/data/

log_dest stdout
